---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - namespace.yaml
  # Pinned deliberately: upgrades are a commit here, not a surprise from upstream.
  - https://raw.githubusercontent.com/argoproj/argo-cd/v3.5.2/manifests/install.yaml
  - ingress.yaml
  - project-beema.yaml
patches:
  - path: insecure.yaml
